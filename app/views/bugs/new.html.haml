#main.container.container-fluid
  #pageContent.col-xs-12
    %h2 Aidez nous en nous signalant les bugs.
      = form_for @bug do |bug|
        .row
          .col-12.col-lg-3
            Livre
          .col-12.col-lg-3
            = @book.name

        .row.row-separation
          .col
            = submit_tag 'Mettre à jour', class: 'form-control'

    #pageFooter.col-xs-12

= hidden_field_tag( :adventure_id, @adventure.id )