# "dev" or "prod" — selects which local Mongo database is used
# (haute_tension_dev / haute_tension_prod). Collections are always "story_pages"
# and "page_views". Leave the database out of MONGO_URI: APP_ENV is what picks it.
APP_ENV=dev
MONGO_URI=mongodb://localhost:27017
